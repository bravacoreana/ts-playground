/*

let a: object = {
  b: "x", //(property) b: string
};

// a2.b; // Property 'b' does not exist on type 'object'.ts(2339)

let b = {
  c: "x",
};

let c = {
  d: {
    e: "f",
  },
};

// c2: d2: {
//   e2: string; //error ts(2693): 'string' only refers to a type, but is being used as a value here.
// }

let f: {
  firstName: string;
  lastName: string;
} = {
  firstName: "John",
  lastName: "Smith",
};

class Person030208 {
  constructor(public firstName: string, public lastName: string) {}
}

f = new Person030208("Matt", "Smith");

*/
